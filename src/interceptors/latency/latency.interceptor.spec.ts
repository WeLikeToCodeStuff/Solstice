import { LatencyInterceptor } from './latency.interceptor';

describe('LatencyInterceptor', () => {
    it('should be defined', () => {
        expect(new LatencyInterceptor()).toBeDefined();
    });
});
